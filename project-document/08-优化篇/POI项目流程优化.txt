1.问题1
    问题背景：数据合并、拆分 多线程
        step1:来一条数据p4要和并库里的A(p1+p2+p3) -> A (p1+p2+p3+p4)
        step2:又来一条删A中的p3 -> A(p1+p2+p4)
        由于两条命令间隔时间短并发执行，导致同时在不同线程执行下，结果变成了 A(p1+p2)
    解决方向：让对   同一条数据的操作，在同一个线程里执行